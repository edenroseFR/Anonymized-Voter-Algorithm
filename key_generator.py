def generate_pubkeys(s_lambda, prog_c):
    """ generate_pubkeys(s_lambda: lambda, prog_c: c program) -> tuple(key1, key2)
    
    Returns a pair of public key that needs to be generated
    once for a given program c.
    """
    # pk = proving_key
    # vk = verification_key
    
    # return (pk, vk)
    pass
